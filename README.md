# Coding Challenge

**Tech stack**: React + TypeScript + Vite + pnpm + Vitest

## Structure
- `EventBus` (publish/subscribe, priority listeners)
- Unit tests and app demo

## How to Run
- `pnpm install`
- `pnpm run dev` — starts demo app
- `pnpm test` — runs unit tests